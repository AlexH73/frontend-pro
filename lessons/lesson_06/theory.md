# Теория: маршрутизация в React

**Маршрутизация** (routing) — это способ сделать в приложении несколько страниц и переключаться между ними без перезагрузки браузера. Например:

-   `/users` → страница со списком пользователей
-   `/comments` → страница с комментариями

В обычном HTML при переходе на другую страницу браузер полностью перезагружает её. В **React** используется библиотека **`react-router-dom`**, которая позволяет переключать компоненты **на стороне клиента** (без перезагрузки).

___

### Основные понятия

1.  **BrowserRouter** Оборачивает всё приложение и включает маршрутизацию.
    
2.  **Routes** Контейнер, внутри которого описываются все маршруты.
    
3.  **Route** Описывает правило: какой путь (`path`) показывает какой компонент (`element`).
    
4.  **Link** Специальная ссылка для перехода между страницами (не перезагружает браузер).
    

___

### Простейший пример

```tsx
import { BrowserRouter, Routes, Route, Link } from "react-router-dom";

function UsersPage() {
  return <h2>Список пользователей</h2>;
}

function CommentsPage() {
  return <h2>Список комментариев</h2>;
}

export default function App() {
  return (
    <BrowserRouter>
      <nav>
        <Link to="/users">Пользователи</Link> |{" "}
        <Link to="/comments">Комментарии</Link>
      </nav>

      <Routes>
        <Route path="/users" element={<UsersPage />} />
        <Route path="/comments" element={<CommentsPage />} />
      </Routes>
    </BrowserRouter>
  );
}
```

___

📌 В этом примере:

-   `/users` → рендерит компонент `UsersPage`
-   `/comments` → рендерит компонент `CommentsPage`
-   `<Link>` работает как `<a>`, но **без перезагрузки страницы**.
